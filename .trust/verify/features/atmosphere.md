# atmosphere — rain engine + city + descent + music + smooth scroll

Owner code: `src/components/Atmosphere.tsx`, `src/lib/rain/engine.ts`, `src/lib/rain/audio.ts`, `src/lib/scroll.ts`, depth tokens in `src/app/globals.css`.

- **EP-RAIN** — canvas animates on load (default: motion ALWAYS on — operator call 2026-08-31 "rain starts instead of having to turn it on"; the OS reduced-motion preference no longer pre-disables it, and only an explicit stored `rain-motion-v2`="off" starts the page still). The storage key is VERSIONED and the old `rain-motion` key is removed on mount: "off" values stored under the old reduced-motion scheme must not keep returning visitors rainless. Drive: load `/`, screenshot twice ≥1s apart; streaks/ripples must differ; water band (bottom ~14%) shows surface line + ripples when drops land. Worst-case default check: context `{ reducedMotion: "reduce" }` + seeded legacy `rain-motion`="off" must STILL load aria-pressed=true, animating, legacy key gone.
- **EP-BLIMP** — skill blimps: one sleek airship at a time crosses the top band (y = max(74px, 9.5% of viewport) — between the nav and the hero eyebrow on every viewport), carrying a lit screen showing ONE skill from `blimpSkills` (content.ts), cycling sequentially with accent colors rotating cyan/magenta/amber/violet. Sprite fully baked per spawn (hull + fins + pods + screen text with baked glow — no runtime shadowBlur; one canvas alive at a time, so no per-skill sprite cache). Direction alternates; speed 16–22 px/s desktop / 12–15 mobile (0.75× hull scale); gap 10–22s between crossings; first spawn ~4s after load; pauses (not despawns) while submerged; skipped when the skills list is empty. Drive: screenshot ~16s after load shows a blimp with legible skill text clear of both nav and hero text at 1600×1000 AND 375×812.
- **EP-CITY** — cyberpunk neon skyline above the waterline on a slim lit embankment (shore band = 3.5% of viewport — raised higher once, pulled back because the towers collided with the hero text). City-glow haze: two baked radial washes (violet 0.34-peak @ 0.68w, cyan 0.05 @ 0.82w, both centered at groundY) fill the sky right-of-center so it never hard-cuts to black — the left stays darkest where the hero text lives. HERO MEGATOWER (replaced the antenna spire 2026-08-31 — operator ask + research docs/research/cyberpunk-skyline/findings.md: an antenna reads as a mast on a normal building, a setback tapered mass is the supertall cue): the tallest near-band tower at 70–88% width grows three baked setback tiers (62%/34%/17% of the mast width, total rise 0.26·viewport into the near band's headroom canvas), violet-shifted fill #241e3f + violet rims (material-contrast cue, no fifth hue), sparse lit floors on the lower tiers, and a crown light pushed into the BEACON array (pulse + water streak for free). Plus ONE baked skybridge in the mid band — a 2.5px deck + cyan light line + 2–3 amber dots between two adjacent near-equal towers (gap 6–30, heights within 30%, both ≥ 45% band) — exactly one, restraint is the look; it may legitimately be absent on seeds where no pair qualifies. Mid band heightFrac raised 0.22→0.25, near 0.15→0.17. Also: three baked parallax bands (windows 70% cyan / 20% amber / 10% magenta), violet haze between bands, rim-lit tower tiers, baked billboards + antenna tip glows + the skybridge deck, TWO live hero signs that flicker (the only runtime shadowBlur), amber promenade lamps + cyan quay lights on the shore, 3–5 pulsing beacons (2–4 mid antennas + the megatower crown), flying cars in two lanes (cap 3, spawn 6–14s). Water: 3-component additive swell (render/collision only — NEVER in the spring integrator), crest-foam dashes at surface slope flips, a slower back parallax water layer, and the flipped skyline reflected in 7px columns that ride each column's surface (alpha 0.35, gone by submerge 0.6). NEON BLEED (operator ask 2026-08-31 "light from the buildings reflects off the water"): every bright source lays a colored streak on the water under `globalCompositeOperation: "lighter"` — baked positions (mid-band billboards 0.5, promenade lamps 0.7 — capped/stride-thinned to 24 desktop / 9 mobile) plus live sources read at draw time (hero signs × their flicker level, beacons × pulse incl. the megatower crown). Each streak hangs from its column's surfaceYs (rides the swell), sways ±1.2px, breathes length/alpha on its own phase, fades with `1 - submerge/0.6`. Streaks run ON MOBILE (cheap gradients) where they are the only reflection layer — the column-blit sprite stays desktop-only. Drive: at scroll 0 the whole skyline sits above the water line with the reflection below it; two screenshots ~10s apart differ (cars, sign flicker, swell phase). Drops land on the VISUAL surface (`surfaceYAt` includes swell), so splashes track the wave crests.
- **EP-SOUND** — two-bed depth-reactive score behind the music toggle (`aria-label="Toggle background music"`). SURFACE world (through the muffle): bright beatless keys Cmaj9→Fmaj9→Am11→Gadd9 (7s per chord, 28s loop; the vi is voiced third-less so it never turns sad), keys lowpass 2400 Hz, sine bass (65.41/87.31/110/98), wow/tremolo, vinyl crackle, C-major-pentatonic bell plucks (C5–C6 pool) through a 0.31s feedback delay, plus light rain. DEEP world (bypasses the muffle, native to the water) is a LOFI GROOVE, not dark ambient: three pad voices as ±7-cent detuned sine PAIRS voicing open root/fifth/octave per chord AN OCTAVE UP from the bass register (roots 261.63/261.63/220/196 — sub-200 Hz pads read as dread, 250–500 Hz reads as warm Rhodes) through a 1400 Hz lowpass whose cutoff wobbles ±35 Hz on a 0.05 Hz LFO, per-voice swell LFOs, two high color voices (B4/D5 over C, gains 0.12/0.10), chords gliding 1.6s. THE BEAT: a 16-step grid derived as CHORD_S/16 per eighth (≈68.6 BPM half-time, phase-locked to the chords, no second clock), kick (sine 130→42 Hz pitch drop, peak 0.16) on 1 and the &-of-3, snare (1500 Hz bandpassed white noise + 200 Hz body, 0.10/0.04) on the backbeat, hats every eighth (6500 Hz bandpass ticks, 0.035, upbeats at 65%) with 58% swing + per-hit ms jitter and velocity variation; drums bypass deepFilter on their own drumSum bus → tanh(1.7) soft-clip shaper → breath; each kick ducks the DRY pad path to 55% (reverb send taps pre-duck so the tail never pumps); snare alone sends 0.2 to the reverb. The kit's level is deepLevel × min(1, abyss/0.15) computed at schedule time — the cursor always advances but hits only schedule once truly under (abyss > 0), so no catch-up burst and the surface stays beatless by construction. Every hit time is clamped to the context clock (max(currentTime + 1ms, swung + jitter)): the drum cursor starts at the context's birth, so a fresh context armed AT DEPTH (visitor reloads while submerged, then clicks) otherwise computes its first hits at t < 0 and `setValueAtTime` THROWS a RangeError (caught live 2026-08-31). Drive: scroll ~2.2 vh deep BEFORE any gesture, click, wait ~3s — no console RangeError and transient drum oscs still accrue. Beat evidence: transient drum oscs accrue ≥3 per ~3s at depth. Abyssal chimes every ~6–15s: three NEAR-harmonic partials 1 : 2 : 3.01 on the chord root ×2, 8ms strike, 1–3.5s decays, on their own UNMUFFLED bus (0.20). A comb-network reverb (4 feedback delays 210/270/330/390ms, feedback ≤0.30, damped at 1800 Hz, wet 0.17 into depthTrim) is fed by pads + chimes + snare sends. WATERLINE ONE-SHOTS: crossing combined depth 0.4 (hysteresis ±0.02, 1s cooldown, armed silently on first setDepth) fires a synthesized plunge going down (lowpass-swept pink noise whoosh 4500→350 Hz + 500→90 Hz bloop + 5 bubble blips, ~0.9s) and a breach coming up (highpass-swept splash 200→5000 Hz + 110→700 Hz whoop + 6 droplet taps, ~1.1s), gain and snappiness scaled by crossing speed, on a dedicated transitionBus joining at MASTER (bypassing muffle AND depthTrim — the live depth automation would wobble a hand-authored envelope). 26 persistent oscillators; plips/plucks/bell partials add transients, so assert `>= 26`. **Default ON + optimistic start** (operator ask 2026-08-31 "sound as soon as I get in"): a fresh visitor's pill is lit before any gesture (`aria-pressed=true`), and ~600ms after mount an OPTIMISTIC enable runs — engines that grant audible autoplay (Chrome media-engagement history, installed PWAs; typically returning visitors) start the score with ZERO interaction, while blocked engines leave the context inaudible (born suspended, or resume() rejected/pending) and the armed qualifying-gesture listeners (pointerdown/keydown/touchend — wheel/scroll can't unlock audio in any engine; a phone's first SWIPE counts via touchend) still start it. The pre-gesture assertion is therefore "no RUNNING context", never "no context". The armed listeners skip clicks on the pill itself so the button's own handler starts (not toggles off) the music; `startTicking` is idempotent (tickTimer guard) because the optimistic pending enable and the gesture enable can both resolve. Drive gotcha: Playwright's Chromium permits autoplay and the `--autoplay-policy=user-gesture-required` flag does NOT reliably override it — emulate blocking in an init script (suspend each context at construction + gate resume() on a real-activation flag), and pin the granted path in a separate default-args launch asserting `state === "running"` with zero gestures. Chord evidence from `__oscs` within the first 7s: 196/246.94/293.66/329.63 ×2 each (Cmaj9 detuned pairs), bass 65.41, deep 261.63/392/523.25 ×2 each, color 493.88/587.33; after +7s the top keys voice moves to 392 (Fmaj9's G4) and bass to 87.31. Toggle off → fade ~1s → state `suspended`. Muffle: the two lowpasses reading ~3000 at surface (of 10 persistent: rain body ~1400 gust-swept, rumble 90, keys 2400, deep 1400 ±35 wobble, four reverb damps 1800, muffle pair — drum/SFX filters are transient per-hit nodes on top) complete the sweep to ~700 by full submersion (combined depth 0.4 ≈ 0.9 viewport of scroll — the floor was raised from 300: a brutal lowpass alone reads as drowning, not mood), hold to the footer, recover at return. Crossfade evidence: snapshot every `__gains` value at the surface, submerge, re-snapshot ONLY indices that existed in the surface snapshot — exactly ONE goes <0.02 → 0.25–0.42 (the deep bus, target 0.32). Never census unslice'd: per-strike abyssal-bell envelopes (peak 0.32, 2–6s decays) are created while submerged and decay through that exact band, so an uncapped census flakes whenever a bell overlaps the read (checker-reproduced live). Rumble rises only to ~0.03 and sparkle dies, so the filter can't confuse them. Loudness: analyser tee on `ctx.destination` (verify.md) → surface RMS ≈ −13 dBFS ±1.5 (MASTER_LEVEL 0.78 — lowered ~3 dB on operator call 2026-08-31: ambience, not a listening session), peak ≈ −4 (the DynamicsCompressor limiter threshold; rarely driven now); below ≈ −16 dBFS is the too-quiet regression. Submerged RMS lands ≈ −18 ± 2 dBFS — calmer than the surface but present (beat + pads replace the muffled keys); near-silence at depth means the deep bus is disconnected. Vinyl crackle is depth-independent — it persists underwater.
- **EP-SCROLL** — wheel + paging keys glide the page through a single 8/s ease (`src/lib/scroll.ts`); there is NO CAMERA — the scene canvas sits IN THE DOCUMENT FLOW (`.scene-doc-canvas`: absolute, 200lvh, z -20) and the browser's compositor scrolls the skyline with the text (operator escalation 2026-08-31: iOS updates window.scrollY asynchronously during flicks, so ANY JS-positioned scene — fixed canvas + translate(-scrollY) included, tried — lags the words by frames and reads as jiggle on real phones; in-flow is the only true glue). The engine paints the scene in document coordinates, clipped to `liveBottom` = max(height, waterRest+220); below that lies a static deep-water column painted once per resize with a fixed-span waterGradient; painting skips when scrollY > liveBottom+40. The DEPTH canvas (`.scene-canvas`: fixed 100lvh, z -10, transparent — must NEVER carry an opaque background) paints only viewport ambience: the abyss wash crossfades in via globalAlpha over ~80px of scroll after the surface clears the frame, then god rays/snow/glints/bubbles. Glued check: (a) scene canvas `rect.top === -scrollY` EXACTLY at any scroll (browser-enforced); (b) motion off, --depth pinned, viewport screenshots at scroll 100 vs 300 must match after a 200css shift in the above-water band (mobile bit-exact; water rows excluded — streak/reflection alphas fade with submerge by design). Re-fixing the scene canvas or repositioning it from JS is THE regression; touch, scrollbar drags, and anchors stay native. Drive: synthetic cancelable WheelEvent → `defaultPrevented` true (false under reduced motion); `page.mouse.wheel(0,120)` ×5 → rAF scrollY trace shows dense small steps, strictly monotonic, total = sum of wheel deltas; PageDown ≈ 0.85×vh; Home glides back to 0 (blur focus first — the key guard ignores focused controls); Space on a focused toggle activates the button and must NOT scroll; anchor click glides via CSS smooth and the smoother adopts the drift — use `a[href="#projects"]`, never `a[href^="#"]` (matches the logo `href="#"`). Gotcha: trace glides from non-zero y — negative-dt backward jerks clamp invisibly at y=0 (see verify.md).
- **EP-DESCENT** — scrolling submerges the camera instead of scrolling the background: within the first ~0.9 viewport the water surface rises past the top of the screen; deeper scroll ramps the scene darker (6-stop ramp to near-black at the footer, never pure black) with god rays near the surface, drifting marine snow, faint abyss glints at depth. DOM panels darken and dim text tiers brighten via `--depth` (0→1). Drive: set `document.documentElement.style.scrollBehavior = "auto"`, then scrollTo 0 / 0.5vh / 1.5vh / mid / bottom with ~0.8s wait before each screenshot; read `getComputedStyle(document.documentElement).getPropertyValue("--depth")` at each stop (monotonic rise); text must stay legible in every screenshot; scroll back to 0 restores the surface scene (city + rain). Coupling: during a wheel glide, `--depth` matches `0.4 · clamp01(scrollY/(0.9·vh))` frame-for-frame (±0.003) — any onset lag vs the text is the water-lags-scroll bug (see verify.md).
- **EP-MOTION-PREF** — reduced-motion users keep NATIVE SCROLLING (scroll.ts still respects the media query; wheel probe NOT prevented) but rain now starts ON for them too (operator call 2026-08-31 — the pill is the opt-out; asserting aria-pressed=false under `{ reducedMotion: "reduce" }` is the OLD contract and would now be a false failure). The rain toggle (`aria-label="Toggle rain animation"`) drives the static-frame path interactively: off → animation halts, static frame remains (city + flat water, no cars); scrolling while static still updates submersion/darkness (re-rendered static frames).
- **EP-PERSIST** — toggles persist. Drive: set rain off, reload, aria-pressed stays false and canvas static; localStorage `rain-motion-v2` = "off" (the unversioned `rain-motion` key is legacy — it is deleted on mount and must never be read).

Gotchas: HEIGHT-ONLY RESIZES must keep the scene rigid: band sprites are bottom-anchored (they slide to the new groundY) while billboards/beacons/mutableWindows carry absolute baked y — `resize()`'s height-only branch shifts those overlays by ΔgroundY and splashes by ΔwaterRest, and preserves the ripple arrays when columnCount is unchanged (reallocating flattened the surface). Regression symptom: neon signs floating in the sky mid-scroll on phones whose browser chrome resizes the viewport (operator screenshot 2026-08-31). Repro harness: mid-scroll `setViewportSize` 390×740→844→740 — signs must stay seated on their towers and the round trip must restore the exact layout. Canvas is `fixed top-0 left-0 w-full -z-10` sized by `.scene-canvas` to **100lvh** (large viewport — NOT `inset-0`/`h-full`: on phones the URL-bar collapse changes the dynamic viewport every scroll direction change, and a rect change sends the engine through `resize()`, which re-derives `waterRest` and reallocates the wave arrays → the mid-scroll water pop the operator reported 2026-08-31; with lvh the rect never moves while scrolling) and carries the page's base coat (`bg-background`); body must stay transparent — `html` has a background (overscroll match), which cancels body→root propagation, so any opaque body background paints over the scene. A flat scene-less screenshot at scroll 0 means that stacking broke (at the footer, near-black is correct). Engine pauses on `visibilitychange`. City is seeded per mount: same skyline across resizes within a visit, new skyline per reload. A suspended AudioContext freezes `currentTime`, so the chord scheduler no-ops safely while off.
