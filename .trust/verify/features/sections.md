# sections — page content

Owner code: `src/app/page.tsx`, `src/components/ProjectCard.tsx`, `src/lib/content.ts`.

- **EP-HOME** — `/` renders hero TOP-LEFT (name, role eyebrow, headline, summary, two CTAs — `justify-start pt-28 md:pt-32`, `min-h-dvh` so iOS toolbar collapse can't push content under the fold) with fixed nav; the sky/city scene owns the lower half of the first viewport. CTAs, contact links, and the floating pills all carry `min-h-11` (≥44px tap targets).
- **EP-ANCHORS** — nav links scroll to `#projects #experience #skills #about #contact`; sections carry `scroll-mt-20` so headings clear the h-14 nav.
- **EP-CARDS** — 8 project cards ordered by career aim (Drydock, trust-stack, FIGHT — Shadow League, Mivora, Document Intelligence, Brain, Echinoid ID, Mother Truckin' Pizza), media side alternating; each card renders ONE current image from its `images: ProjectImage[]` array via next/image with `sizes="(min-width: 768px) 50vw, 100vw"` (so `#projects img` count === card count, not total image count); declared dims in content.ts must match the real files; no Sport IQ strip (removed 2026-08-31); no lesson lines (added then removed same day by operator call); **no Visit-live links** (removed 2026-08-31 by operator call — GitHub links + notes remain, and `liveUrl`/`terminal` are gone from the Project type, so any reappearance is a regression).
- **EP-GALLERY** — multi-image demos (operator ask 2026-08-31): cards with >1 image show ‹ › arrow buttons (`aria-label="Previous image"`/`"Next image"`) and an "n / m" counter chip; arrows wrap modulo count; single-image cards show no arrows. Clicking the image (`aria-label="Expand {name} image"`) opens a lightbox: `role="dialog" aria-modal`, backdrop click + Escape close it, ArrowLeft/ArrowRight page inside it, body scroll locks while open and RESTORES on close (the classic leak: `documentElement.style.overflow` left `"hidden"`), alt-text caption under the expanded image. Image-set intent: the lead image is the most impressive card-legible overview; dense diagrams (rag-detail, truststack-grid) live later in the list, meant to be read expanded. Current sets — Drydock 1 (editor capture pending), trust-stack 2 (flight-console v3 lead + 2×2 grid), FIGHT 3 (bout lead + Film Room + mode select), Mivora 2 (landing + Bit-vs-Qubit lesson page), Document Intelligence 2 (5-stage overview + dense pipeline detail), Brain 1 (the ~40-label vault graph — operator explicitly prefers the ORIGINAL brain.png; never regenerate it), Echinoid 2 (landing + photo-to-ID process), Pizza 3 (home lead + menu board + catering). Drive: on the Document Intelligence card click Next → rendered `src` flips rag→rag-detail; open lightbox, Escape, assert overflow restored. Gallery clicks/keys are qualifying audio gestures — run gallery checks AFTER audio arming assertions.
- **EP-EDPX** — experience section carries two education cards: UT Knoxville (B.S. Applied AI, expected 2028) and UCF (CS core with named coursework, GPA 3.58). Contact lists email, phone (tel:+12283572131 — operator explicitly approved publishing 228-357-2131 on 2026-08-31, reversing the earlier do-not-publish note), GitHub, LinkedIn.
- **EP-MOBILE** — mobile preset (375×812): nav shows name only (links hidden by design), cards stack vertically, no horizontal scroll, controls bottom-right don't cover the footer text at rest.

Gotchas: content facts live in `src/lib/content.ts` only — copy edits happen there, never inline in JSX. Echinoid card must never carry an accuracy percentage (recorded honesty mandate).
