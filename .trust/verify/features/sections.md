# sections — page content

Owner code: `src/app/page.tsx`, `src/components/ProjectCard.tsx`, `src/lib/content.ts`.

- **EP-HOME** — `/` renders hero TOP-LEFT (name, role eyebrow, headline, summary, two CTAs — `justify-start pt-28 md:pt-32`, `min-h-screen`) with fixed nav; the sky/city scene owns the lower half of the first viewport. The hero is sized with the STABLE viewport unit (100vh = large viewport on phones) and dynamic-viewport units are BANNED from the whole page (don't even write that class token in comments or docs — Tailwind scans everything non-ignored and re-emits the utility): the dynamic unit shrinks while the browser toolbar is visible, which pulled the projects section up to the waterline on iOS Safari and re-laid-out the document on every toolbar collapse mid-scroll (operator screenshots 2026-08-31). Guard: layout-stability check asserts zero dvh/svh strings in the built CSS AND `#projects` doc-top ≥ waterRest + 90css at 390×844. CTAs, contact links, and the floating pills all carry `min-h-11` (≥44px tap targets).
- **EP-ANCHORS** — nav links scroll to `#projects #experience #skills #about #contact`; sections carry `scroll-mt-20` so headings clear the h-14 nav.
- **EP-CARDS** — 9 project cards ordered by career aim (Drydock, trust-stack, Cozy, FIGHT — Shadow League, Mivora, Document Intelligence, Brain, Echinoid ID, Mother Truckin' Pizza — Cozy added 2026-09-01, slotted third as dead-center agent work), media side alternating; each card renders ONE current image from its `images: ProjectImage[]` array as a plain `<img>` with a hand-built srcset — every asset is a WebP PAIR (`foo.webp` full width + `foo-sm.webp` 800w, generated at ~q0.85; 12.5MB of PNGs became 2MB total, the fix for the real-phone "white cards + lag" report 2026-08-31) with `sizes="(min-width: 768px) 50vw, 100vw"`, `loading="lazy" decoding="async"` (so `#projects img` count === card count, not total image count); next/image is NOT used here (it can't emit srcset under `unoptimized`); adding an image means generating BOTH variants; declared dims in content.ts must match the real full-size files; no Sport IQ strip (removed 2026-08-31); no lesson lines (added then removed same day by operator call); **no Visit-live links** (removed 2026-08-31 by operator call — GitHub links + notes remain, and `liveUrl`/`terminal` are gone from the Project type, so any reappearance is a regression).
- **EP-GALLERY** — multi-image demos (operator ask 2026-08-31): cards with >1 image show ‹ › arrow buttons (`aria-label="Previous image"`/`"Next image"`) and an "n / m" counter chip; arrows wrap modulo count; single-image cards show no arrows. Clicking the image (`aria-label="Expand {name} image"`) opens a lightbox: `role="dialog" aria-modal`, backdrop click + Escape close it, ArrowLeft/ArrowRight page inside it, body scroll locks while open and RESTORES on close (the classic leak: `documentElement.style.overflow` left `"hidden"`), alt-text caption under the expanded image. Image-set intent: each project leads with its best overview, then walks its real features (inside-the-app captures, 2026-08-31 batch). Current sets (28 images total) — Drydock 3 (Chat tab lead w/ drafted prompt + automations shelf + Invoice Reader detail sheet, ALL on the same bundled "Dusk Ridge" wallpaper — operator wants the set background-consistent), Cozy 3 (hearth-workspace lead with Mission Control + delegated terminal, chat-tab morning briefing with the approval-gate line, terminal running the real 20-file logic suite green — captured 2026-09-01 from the REAL app via an isolated harness: scratch vault + isolated data dirs seeded before boot, staged over the exposed store; NEVER screenshot Cozy against the real ~/Projects/brain vault or real userData — settings.json must set brainRoot to a throwaway; Finance panel stays out of frame, real balances), trust-stack 2 (flight-console lead + 2×2 grid; CHECKS icon is a single circle with the check fully inside — the two-circle tangle was operator-rejected), FIGHT 5 (bout lead + front-lit ATLAS-R robot + Practice-vs-ghost + Loadout hub + mode select; the rear-view Film Room shot was operator-removed), Mivora 5 (home lead — operator restored it after trying inside-first — + track path + mastery quiz + lesson page + progress dashboard), Document Intelligence 2 (dense architecture LEADS, 5-stage overview second — operator-swapped), Brain 1 (the ~40-label vault graph — operator explicitly prefers the ORIGINAL brain.png; never regenerate it), Echinoid 4 (home lead restored + identify flow + species database + photo-to-ID process; the mock-mode results capture was rejected for its "Mock mode" banners), Pizza 3 (home lead + menu board + catering). Drive: on the Document Intelligence card click Next → rendered `src` flips rag-detail→rag; open lightbox, Escape, assert overflow restored. Gallery clicks/keys are qualifying audio gestures — run gallery checks AFTER audio arming assertions.
- **EP-EDPX** — experience section carries two education cards: UT Knoxville (B.S. Applied AI, expected 2028) and UCF (CS core with named coursework, GPA 3.58). Contact lists email, phone (tel:+12283572131 — operator explicitly approved publishing 228-357-2131 on 2026-08-31, reversing the earlier do-not-publish note), GitHub, LinkedIn.
- **EP-MOBILE** — mobile preset (375×812): nav shows name only (links hidden by design), cards stack vertically, no horizontal scroll, controls bottom-right don't cover the footer text at rest. Card text COLLAPSES on mobile (operator ask 2026-08-31): below md, each card shows only images + name/badge + accent tagline + an "Expand project ▾" pill (`aria-expanded`, `aria-controls`, min-h-11); description/points/stack/links live in a `hidden md:block` wrapper toggled per-card, starting collapsed. Desktop must be unaffected (details always visible, pill `md:hidden`). Drive: at 375px assert description hidden → toggle → visible → toggle → hidden; at 1600px assert description visible and pill invisible.

Gotchas: content facts live in `src/lib/content.ts` only — copy edits happen there, never inline in JSX. Echinoid card must never carry an accuracy percentage (recorded honesty mandate).
